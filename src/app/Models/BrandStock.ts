export interface BrandStock {
  category: string;
  totalStock: number;
}
export interface ProductStock {
  product: string;
  totalStock: number;
}
export interface Particulars {
  Id: number;
  name: string;
}
